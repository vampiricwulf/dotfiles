mkdir ./tempfonts;
cd ./tempfonts;
attach=$(mkvmerge -i "$1" | grep Attachment | awk -F" " '{print $3}' | sed 's/://');
mkvextract attachments "$1" $attach;
# You'll need to change the font directory for non-Mac OS X
rsync -av --remove-source-files ./ ~/Library/Fonts/;
cd ../;
rm ./tempfonts;
