if [ "$1" = "--help" ] || [ "$1" = "-help" ] || [ "$1" = "" ]; then
	echo "Usage: vid24chanwebm input output-name start-time end-time";
else
	ffmpeg -i "$1" -ss "$3" -to "$4" -c:v libvpx -crf 4 -b:v 1500K -vf scale=640:-1 -an "$2".webm;
fi